name: cryptalk
description: "Cryptalk 加密聊天应用."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  device_info_plus: ^12.3.0
  dio: ^5.9.1
  flutter:
    sdk: flutter
  gal: ^2.3.2
  image_picker: ^1.2.1
  logger: ^2.6.2
  lpinyin: ^2.0.3
  mobile_scanner: ^7.2.0
  package_info_plus: ^9.0.0
  path: ^1.8.3
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.4
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.4+4
  web_socket_channel: ^3.0.3

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
